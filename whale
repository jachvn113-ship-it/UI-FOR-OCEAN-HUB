-- ============================================
-- AUTO WHALE v3 — FLY-OVER-WALL
-- Bay lên trời vượt tường, không bị kẹt
-- ============================================

local Players           = game:GetService("Players")
local RunService        = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LP   = Players.LocalPlayer
local char = LP.Character or LP.CharacterAdded:Wait()
local hrp  = char:WaitForChild("HumanoidRootPart")
local hum  = char:WaitForChild("Humanoid")

local Remote      = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Input")
local WhaleFolder = workspace:WaitForChild("Whale")

-- ============ CONFIG ============
local SPEED          = 200
local STOP_DISTANCE  = 50
local SCAN_INTERVAL  = 0.5

local SAFE_HEIGHT    = 80     -- cao hơn tường bao nhiêu studs (so với whale)
local HIGH_Y_MIN     = 150    -- độ cao Y tối thiểu khi bay
local H_ARRIVE_XZ    = 40     -- khi XZ cách whale < 40 studs thì bắt đầu hạ

-- ============ STATE ============
local ENABLED = false
local currentTarget = nil
local lastScan      = 0

-- ============ HELPER ============
local function getModelPosition(model)
    if not model or not model.Parent then return nil end
    if model.PrimaryPart then return model.PrimaryPart.Position end
    local part = model:FindFirstChildWhichIsA("BasePart", true)
    return part and part.Position or nil
end

local function sendDash(cf) Remote:FireServer("Dash", cf) end

local function findNearestWhale()
    local myPos = hrp.Position
    local best, bestDist = nil, math.huge
    for _, model in ipairs(WhaleFolder:GetChildren()) do
        if model:IsA("Model") then
            local pos = getModelPosition(model)
            if pos then
                local d = (pos - myPos).Magnitude
                if d < bestDist then best, bestDist = model, d end
            end
        end
    end
    return best
end

local function scanFolder(force)
    local now = tick()
    if not force and now - lastScan < SCAN_INTERVAL then return end
    lastScan = now
    local newTarget = findNearestWhale()
    if newTarget and newTarget ~= currentTarget then
        currentTarget = newTarget
    elseif not newTarget then
        currentTarget = nil
    end
end

WhaleFolder.ChildAdded:Connect(function(child)
    if child:IsA("Model") and ENABLED then
        currentTarget = child
    end
end)

-- ============ ON/OFF ============
local function setEnabled(state)
    ENABLED = state
    local c = LP.Character
    if c then
        local h = c:FindFirstChildOfClass("Humanoid")
        local r = c:FindFirstChild("HumanoidRootPart")
        if h then
            h.PlatformStand = state
            h.WalkSpeed     = state and 0 or 16
            h.JumpPower     = state and 0 or 50
            h.UseJumpPower  = true
        end
        if r then
            r.AssemblyLinearVelocity  = Vector3.zero
            r.AssemblyAngularVelocity = Vector3.zero
        end
    end
end

-- ============ UI ============
local function createUI()
    local gui = Instance.new("ScreenGui")
    gui.Name = "AutoWhaleUI"
    gui.ResetOnSpawn = false
    gui.Parent = LP:WaitForChild("PlayerGui")

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 180, 0, 60)
    frame.Position = UDim2.new(0, 20, 0, 100)
    frame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Draggable = true
    frame.Parent = gui
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)
    local stroke = Instance.new("UIStroke", frame)
    stroke.Color = Color3.fromRGB(60, 60, 70)
    stroke.Thickness = 1.5

    local title = Instance.new("TextLabel", frame)
    title.Size = UDim2.new(1, 0, 0, 25)
    title.Position = UDim2.new(0, 0, 0, 4)
    title.BackgroundTransparency = 1
    title.Text = "🐋 AUTO WHALE v3"
    title.Font = Enum.Font.GothamBold
    title.TextSize = 13
    title.TextColor3 = Color3.fromRGB(240, 240, 240)

    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(1, -20, 0, 26)
    btn.Position = UDim2.new(0, 10, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    btn.BorderSizePixel = 0
    btn.Text = "OFF"
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    btn.MouseButton1Click:Connect(function()
        setEnabled(not ENABLED)
        if ENABLED then
            btn.Text = "ON"
            btn.BackgroundColor3 = Color3.fromRGB(50, 180, 90)
            scanFolder(true)
        else
            btn.Text = "OFF"
            btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        end
    end)

    return gui
end

createUI()

-- ============ MAIN LOOP ============
RunService.Heartbeat:Connect(function(dt)
    if not char.Parent or not hrp.Parent then
        char = LP.Character or LP.CharacterAdded:Wait()
        hrp  = char:WaitForChild("HumanoidRootPart")
        hum  = char:WaitForChild("Humanoid")
        if ENABLED then hum.PlatformStand = true; hum.WalkSpeed = 0 end
        return
    end

    if not ENABLED then return end

    scanFolder(false)
    if not currentTarget or not currentTarget.Parent then
        currentTarget = nil
        return
    end

    local targetPos = getModelPosition(currentTarget)
    if not targetPos then return end

    local myPos = hrp.CFrame.Position

    -- Tính vector tới whale
    local flatTarget = Vector3.new(targetPos.X, myPos.Y, targetPos.Z)
    local flatDist   = (flatTarget - myPos).Magnitude
    local safeY      = math.max(targetPos.Y + SAFE_HEIGHT, HIGH_Y_MIN)

    local moveTarget
    if myPos.Y < safeY - 5 then
        -- GIAI ĐOẠN 1: bay thẳng lên tới safeY
        moveTarget = Vector3.new(myPos.X, safeY, myPos.Z)
    elseif flatDist > H_ARRIVE_XZ then
        -- GIAI ĐOẠN 2: bay ngang trên cao tới whale (giữ safeY)
        moveTarget = Vector3.new(targetPos.X, safeY, targetPos.Z)
    else
        -- GIAI ĐOẠN 3: hạ xuống whale
        moveTarget = Vector3.new(targetPos.X, targetPos.Y + STOP_DISTANCE * 0.3, targetPos.Z)
    end

    local diff     = moveTarget - myPos
    local distance = diff.Magnitude

    -- Đến đủ gần whale (3D) thì dừng
    local trueDistToWhale = (targetPos - myPos).Magnitude
    if trueDistToWhale <= STOP_DISTANCE then
        return
    end

    if distance < 0.5 then return end

    local dir  = diff.Unit
    local step = math.min(SPEED * dt, distance)
    local newPos = myPos + dir * step
    local newCF  = CFrame.new(newPos) * (hrp.CFrame - hrp.CFrame.Position)

    hrp.CFrame = newCF
    hrp.AssemblyLinearVelocity  = Vector3.zero
    hrp.AssemblyAngularVelocity = Vector3.zero

    sendDash(newCF)
end)

print("[AutoWhale v3] Loaded — click ON để bật (Fly-over-wall mode)")
