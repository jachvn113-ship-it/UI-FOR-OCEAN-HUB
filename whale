-- ============================================
-- AUTO WHALE v5 — LAND ON TOP (không đâm vào whale)
-- ============================================

local Players           = game:GetService("Players")
local RunService        = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LP   = Players.LocalPlayer
local char = LP.Character or LP.CharacterAdded:Wait()
local hrp  = char:WaitForChild("HumanoidRootPart")
local hum  = char:WaitForChild("Humanoid")

local Remote      = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Input")
local WhaleFolder = workspace:WaitForChild("Whale")

-- ============ CONFIG ============
local SPEED           = 200
local SCAN_INTERVAL   = 0.5

local STOP_XZ         = 50    -- bán kính XZ dừng
local STOP_HEIGHT     = 15    -- sai lệch Y tối đa so với đỉnh whale
local HOVER_OFFSET    = 8     -- bay cao hơn đỉnh whale 8 studs (tránh đâm)

local SAFE_HEIGHT     = 60    -- bay cao hơn đỉnh whale khi vượt tường
local HIGH_Y_MIN      = 120
local H_ARRIVE_XZ     = 80    -- XZ < 80 thì bắt đầu hạ
local DESCEND_SPEED   = 350
local SEND_INTERVAL   = 0.05  -- rate-limit gửi remote

-- ============ STATE ============
local ENABLED = false
local currentTarget = nil
local lastScan = 0
local lastSend = 0
local phase = "idle"

-- ============ HELPER ============
-- Lấy thông tin whale: tâm + đỉnh + kích thước
local function getWhaleInfo(model)
    if not model or not model.Parent then return nil end
    local ok, cf, size = pcall(function()
        local c, s = model:GetBoundingBox()
        return c, s
    end)
    if ok and cf and size then
        return {
            center = cf.Position,
            top    = cf.Position.Y + size.Y / 2,
            size   = size
        }
    end
    -- fallback nếu GetBoundingBox lỗi
    local part = model:FindFirstChildWhichIsA("BasePart", true)
    if not part then return nil end
    return {
        center = part.Position,
        top    = part.Position.Y + part.Size.Y / 2,
        size   = part.Size
    }
end

local function sendDash(cf)
    local now = tick()
    if now - lastSend >= SEND_INTERVAL then
        Remote:FireServer("Dash", cf)
        lastSend = now
    end
end

local function findNearestWhale()
    local myPos = hrp.Position
    local best, bestDist = nil, math.huge
    for _, model in ipairs(WhaleFolder:GetChildren()) do
        if model:IsA("Model") then
            local info = getWhaleInfo(model)
            if info then
                local d = (info.center - myPos).Magnitude
                if d < bestDist then best, bestDist = model, d end
            end
        end
    end
    return best
end

local function scanFolder(force)
    local now = tick()
    if not force and now - lastScan < SCAN_INTERVAL then return end
    lastScan = now
    local newTarget = findNearestWhale()
    if newTarget and newTarget ~= currentTarget then
        currentTarget = newTarget
    elseif not newTarget then
        currentTarget = nil
    end
end

WhaleFolder.ChildAdded:Connect(function(child)
    if child:IsA("Model") and ENABLED then
        currentTarget = child
    end
end)

-- ============ ON/OFF ============
local function setEnabled(state)
    ENABLED = state
    phase = "idle"
    local c = LP.Character
    if c then
        local h = c:FindFirstChildOfClass("Humanoid")
        local r = c:FindFirstChild("HumanoidRootPart")
        if h then
            h.PlatformStand = state
            h.WalkSpeed     = state and 0 or 16
            h.JumpPower     = state and 0 or 50
            h.UseJumpPower  = true
        end
        if r then
            r.AssemblyLinearVelocity  = Vector3.zero
            r.AssemblyAngularVelocity = Vector3.zero
        end
    end
end

-- ============ UI ============
local function createUI()
    local gui = Instance.new("ScreenGui")
    gui.Name = "AutoWhaleUI"
    gui.ResetOnSpawn = false
    gui.Parent = LP:WaitForChild("PlayerGui")

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 200, 0, 85)
    frame.Position = UDim2.new(0, 20, 0, 100)
    frame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Draggable = true
    frame.Parent = gui
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)
    local stroke = Instance.new("UIStroke", frame)
    stroke.Color = Color3.fromRGB(60, 60, 70)
    stroke.Thickness = 1.5

    local title = Instance.new("TextLabel", frame)
    title.Size = UDim2.new(1, 0, 0, 25)
    title.Position = UDim2.new(0, 0, 0, 4)
    title.BackgroundTransparency = 1
    title.Text = "🐋 AUTO WHALE v5"
    title.Font = Enum.Font.GothamBold
    title.TextSize = 13
    title.TextColor3 = Color3.fromRGB(240, 240, 240)

    local status = Instance.new("TextLabel", frame)
    status.Size = UDim2.new(1, -20, 0, 14)
    status.Position = UDim2.new(0, 10, 0, 28)
    status.BackgroundTransparency = 1
    status.Text = "Trạng thái: idle"
    status.Font = Enum.Font.Gotham
    status.TextSize = 11
    status.TextColor3 = Color3.fromRGB(160, 160, 170)
    status.TextXAlignment = Enum.TextXAlignment.Left

    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(1, -20, 0, 26)
    btn.Position = UDim2.new(0, 10, 0, 48)
    btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    btn.BorderSizePixel = 0
    btn.Text = "OFF"
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    btn.MouseButton1Click:Connect(function()
        setEnabled(not ENABLED)
        if ENABLED then
            btn.Text = "ON"
            btn.BackgroundColor3 = Color3.fromRGB(50, 180, 90)
            scanFolder(true)
        else
            btn.Text = "OFF"
            btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        end
    end)

    return gui, status
end

local ui, statusLabel = createUI()

-- ============ MAIN LOOP ============
RunService.Heartbeat:Connect(function(dt)
    if not char.Parent or not hrp.Parent then
        char = LP.Character or LP.CharacterAdded:Wait()
        hrp  = char:WaitForChild("HumanoidRootPart")
        hum  = char:WaitForChild("Humanoid")
        if ENABLED then hum.PlatformStand = true; hum.WalkSpeed = 0 end
        return
    end

    if not ENABLED then
        statusLabel.Text = "Trạng thái: OFF"
        return
    end

    scanFolder(false)
    if not currentTarget or not currentTarget.Parent then
        currentTarget = nil
        statusLabel.Text = "Trạng thái: chờ whale..."
        return
    end

    local info = getWhaleInfo(currentTarget)
    if not info then return end

    local myPos    = hrp.Position
    local whaleTop = info.top
    local targetY  = whaleTop + HOVER_OFFSET    -- điểm hạ cánh = trên đỉnh whale

    -- Khoảng cách XZ (không tính Y)
    local dxz = Vector3.new(info.center.X - myPos.X, 0, info.center.Z - myPos.Z)
    local flatDist = dxz.Magnitude

    -- Khoảng cách Y tới điểm hạ cánh
    local dy = myPos.Y - targetY

    -- ============ STOP CONDITION ============
    -- Đã ở trên đầu whale và ngang tầm đỉnh
    if flatDist <= STOP_XZ and math.abs(dy) <= STOP_HEIGHT then
        statusLabel.Text = string.format("✅ ĐÃ TỚI (XZ:%.0f | Y:%.0f)", flatDist, dy)
        return
    end

    -- ============ QUYẾT ĐỊNH HƯỚNG ============
    local moveTarget, speed

    if flatDist > H_ARRIVE_XZ then
        -- CÒN XA: bay cao vượt tường
        local safeY = math.max(whaleTop + SAFE_HEIGHT, HIGH_Y_MIN)
        if myPos.Y < safeY - 10 then
            -- BAY LÊN
            phase = "rise"
            speed = SPEED
            moveTarget = Vector3.new(myPos.X, safeY, myPos.Z)
        else
            -- BAY NGANG TRÊN CAO
            phase = "fly"
            speed = SPEED
            moveTarget = Vector3.new(info.center.X, safeY, info.center.Z)
        end
    else
        -- ĐÃ GẦN XZ: HẠ XUỐNG TỚI ĐỈNH WHALE
        phase = "descend"
        speed = DESCEND_SPEED
        moveTarget = Vector3.new(info.center.X, targetY, info.center.Z)
    end

    statusLabel.Text = string.format("%s | XZ:%.0f | Y:%.0f", phase, flatDist, dy)

    -- ============ DI CHUYỂN ============
    local diff     = moveTarget - myPos
    local distance = diff.Magnitude

    if distance < 0.5 then return end

    local dir  = diff.Unit
    local step = math.min(speed * dt, distance)
    local newPos = myPos + dir * step
    local newCF  = CFrame.new(newPos) * (hrp.CFrame - hrp.CFrame.Position)

    hrp.CFrame = newCF
    hrp.AssemblyLinearVelocity  = Vector3.zero
    hrp.AssemblyAngularVelocity = Vector3.zero

    sendDash(newCF)
end)

print("[AutoWhale v5] Loaded — click ON để bật")
